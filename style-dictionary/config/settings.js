const THEMES = [
    'cycloid',
    'cycloid-dark'
];

// Prefix Lookup Table - define your library prefixes here
const LIBRARIES = [
    { id: 'vuetify4', prefix: 'v' },
];

export {
    THEMES,
    LIBRARIES
}